apiVersion: v1
kind: Pod
metadata:
  name: cdr-testing
  labels:
    app: alpine
spec:
  containers:
  - name: alpine-test1
    image: alpine:latest
    command: ["/bin/sh", "-c", "wget -q https://example.com/package.tar.gz -O /tmp/package.tar.gz && sleep 3600"]